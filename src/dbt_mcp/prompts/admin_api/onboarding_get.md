Returns the current onboarding record for the account, or null if no onboarding has been started yet.

Use this to check whether the user has an active onboarding, and what resources have already been configured (project, connection, repository, environments, etc.).

Call this first before starting any onboarding conversation to detect a resumable session.
