## v2.3.1 - 2026-09-11
### Bug Fix
* Infer account prefix from DBT_HOST when the dbt Platform account API returns null subdomain fields, preventing DNS errors on discovery and semantic layer tools for multi-cell accounts
* Fix get_job_run_artifacts returning 500 server error instead of 400 client errors when a jq filter fails validation
