## v2.4.0 - 2026-09-22
### Enhancement or New Feature
* Add an optional project_id filter to list_jobs to retrieve jobs across all environments in a project, while preserving the configured environment default when omitted.
