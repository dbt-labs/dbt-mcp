## v1.22.1 - 2026-07-20
### Under the Hood
* Bump the pinned Node version to 22.18.0 in .tool-versions so the ui/ ESLint 9 flat-config check runs locally (Node 20.17 hits ERR_VM_DYNAMIC_IMPORT_CALLBACK_MISSING).
### Bug Fix
* Fix manifest path resolution when DBT_PROJECT_DIR is a relative path
* Fix get_lineage 400 Bad Request caused by requesting description field not present in the Discovery API lineage GraphQL schema
