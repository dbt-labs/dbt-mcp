## v2.2.0 - 2026-08-25
### Enhancement or New Feature
* Add an optional `dbt_version_override` parameter to `trigger_job_run`, so a run can be executed on a different dbt version without changing the job or its environment.
